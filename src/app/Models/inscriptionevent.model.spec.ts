import { Inscriptionevent } from './inscriptionevent.model';

describe('Inscriptionevent', () => {
  it('should create an instance', () => {
    expect(new Inscriptionevent()).toBeTruthy();
  });
});
